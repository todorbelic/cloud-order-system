apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: catalog-db-pvc
  namespace: cloud-order-system
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: catalog-db
  namespace: cloud-order-system
spec:
  serviceName: catalog-db-service
  replicas: 1
  selector:
    matchLabels:
      app: catalog-db
  template:
    metadata:
      labels:
        app: catalog-db
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "catalogdb"
            - name: POSTGRES_USER
              value: "cataloguser"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: catalog-db-secret
                  key: CATALOG_DB_PASSWORD
          volumeMounts:
            - name: catalog-db-storage
              mountPath: /var/lib/postgresql/data
            - name: init-sql
              mountPath: /docker-entrypoint-initdb.d
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "cataloguser", "-d", "catalogdb"]
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: catalog-db-storage
          persistentVolumeClaim:
            claimName: catalog-db-pvc
        - name: init-sql
          configMap:
            name: catalog-db-init
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-db-service
  namespace: cloud-order-system
spec:
  selector:
    app: catalog-db
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None  
